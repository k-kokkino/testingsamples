<Project>
  <ImportGroup Condition="Exists($([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))) == 'true'">
    <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />
  </ImportGroup>

  <PropertyGroup Label="Package">
    <PackageProjectUrl>https://github.com/k-kokkino/testingsamples</PackageProjectUrl>
    <RepositoryUrl>https://github.com/k-kokkino/testingsamples</RepositoryUrl>
  </PropertyGroup>
</Project>
